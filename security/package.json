{
  "name": "daily-security-tests",
  "private": true,
  "description": "Tests de las reglas de seguridad de Firestore contra el emulador.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-daily \"node rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-tools": "^13.0.0"
  }
}
